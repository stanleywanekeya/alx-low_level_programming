#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: comand line count
 * @argv: command line vector
 *Return: value 0 for success
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
